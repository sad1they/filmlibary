/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'm.media-amazon.com', 
            'avatars.mds.yandex.net'
        ],
    },
}

module.exports = nextConfig
